/**
 * Surfaces aligned with `DashboardShell` page gradient and sidebar glass
 * (`from-primary-500/10 via-white to-white` / dark gray stack).
 */
export const DASHBOARD_SURFACE =
  'rounded-2xl border border-gray-200/70 bg-white/70 text-foreground shadow-sm backdrop-blur-lg dark:border-gray-800/60 dark:bg-gray-950/40';

/** Slightly softer inset panels (e.g. agent rows inside a section). */
export const DASHBOARD_SURFACE_NESTED =
  'rounded-xl border border-gray-200/60 bg-white/50 shadow-none backdrop-blur-md dark:border-gray-800/50 dark:bg-gray-950/30';
